# TEST STRATEGY

Layers: domain unit tests; seed-integrity tests; UI interaction tests; browser responsive QA; PWA offline/update; accessibility.

Golden scenarios: negative claim, physical material with DO, progress work without DO, late AR, cost overrun, subcon overclaim, retention release.
