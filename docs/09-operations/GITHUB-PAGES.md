# GITHUB PAGES

Target repository URL shape: `https://<user>.github.io/construction-project-erp-pwa/`.

Configure Vite base path, manifest URLs and service-worker scope for repository-subpath hosting. Avoid hardcoded `/assets/...` paths. Verify the actual published URL, not only localhost.
